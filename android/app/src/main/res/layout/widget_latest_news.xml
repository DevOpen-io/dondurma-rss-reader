<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_background"
    android:padding="14dp">

    <!-- Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="8dp">
        <ImageView
            android:layout_width="4dp"
            android:layout_height="16dp"
            android:background="@drawable/widget_accent_bar"
            android:layout_marginEnd="10dp"/>
        <TextView
            android:id="@+id/header_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Son Eklenenler"
            android:textColor="#FFFFFFFF"
            android:textSize="13sp"
            android:textStyle="bold"
            android:letterSpacing="0.02"/>
        <TextView
            android:id="@+id/header_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#6612A8FF"
            android:textSize="10sp"/>
    </LinearLayout>

    <!-- List + empty overlap in a FrameLayout so the ListView always gets the
         full remaining height. A match_parent empty view placed as a plain
         LinearLayout sibling steals the weighted list's space on some launchers,
         collapsing it to 0px (list renders blank despite holding items). -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <ListView
            android:id="@+id/widget_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:divider="#1AFFFFFF"
            android:dividerHeight="0.5dp"
            android:scrollbars="none"/>

        <TextView
            android:id="@+id/empty_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:text="Henüz haber yok"
            android:textColor="#66FFFFFF"
            android:textSize="11sp"/>

    </FrameLayout>

</LinearLayout>
