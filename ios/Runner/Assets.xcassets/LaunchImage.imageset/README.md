# Dondurma Launch Screen Assets

You can customize the launch screen with your own desired assets by replacing the image files in this directory.

Open `ios/Runner.xcworkspace` in Xcode, select `Runner/Assets.xcassets`, and replace these images. Keep branding consistent with `assets/Logo.png`, the launcher-icon source.
